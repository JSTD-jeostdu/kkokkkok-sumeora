# 이어달리기 체인 CTA 강화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 결과 화면(`#playend-panel`)에서 "나도 숨기기" 버튼을 단독 주역으로 격상시켜 플레이어→제작자 전환율을 높인다.

**Architecture:** 순수 HTML 마크업 재배치 + CSS 스타일·애니메이션 추가. 기존 JS 클릭 핸들러와 id는 전혀 건드리지 않는다 (`btn-play-make`, `btn-share-card`, `btn-play-home`, `btn-open-delete-play` id 유지).

**Tech Stack:** 순수 HTML/CSS (빌드 도구 없음)

## Global Constraints
- 단일 파일 `index.html` 원칙 — 이번 작업은 `index.html` 내부 수정만 발생, 신규 파일 없음
- 디자인 토큰: 도화지 `#FAF7F0` · 연필 `#4A4A4A` · 포인트 레드(`--accent`) `#E45B5B`
- 기존 전역 `@media (prefers-reduced-motion: reduce)` 규칙(`index.html:345-347`)이 모든 `animation`을 무력화하므로, 신규 애니메이션에 별도 예외 처리 불필요
- JS 로직·Firestore 데이터 모델 변경 없음

---

### Task 1: 결과 화면 CTA 레이아웃·문구·애니메이션 적용

**Files:**
- Modify: `C:\Users\jeodu\Desktop\VibeCoding\kkokkkok-sumeora\index.html:534-541` (`#playend-panel` 버튼 영역)
- Modify: `C:\Users\jeodu\Desktop\VibeCoding\kkokkkok-sumeora\index.html:318` 직후 (CSS, `.eyedropper-badge` 규칙 다음)

**Interfaces:**
- Consumes: 기존 `.btn`, `.btn-primary`, `.btn-ghost`, `.btn-wide`, `.link-btn`, `.panel-actions` CSS 클래스와 `--accent`/`--shadow` 토큰
- Produces: 없음 (최종 UI 변경, 다른 태스크가 참조하지 않음)

- [ ] **Step 1: HTML 마크업 재배치**

`index.html:534-541`의 현재 내용:

```html
      <div class="panel-actions">
        <button class="btn" id="btn-share-card">결과 카드 공유</button>
        <button class="btn btn-primary" id="btn-play-make">나도 숨기기</button>
      </div>
      <div class="panel-actions" style="margin-top:8px;">
        <button class="btn btn-ghost" id="btn-play-home">홈으로</button>
      </div>
      <button class="link-btn" id="btn-open-delete-play">스테이지 삭제</button>
```

다음으로 교체:

```html
      <p class="relay-hook">이번엔 당신 차례예요.<br>친구를 <b>제대로</b> 골탕먹여보세요 😏</p>
      <button class="btn btn-primary btn-wide relay-cta" id="btn-play-make">나도 숨기기</button>
      <div class="panel-actions" style="margin-top:8px;">
        <button class="btn btn-ghost" id="btn-play-home">홈으로</button>
      </div>
      <div class="panel-links-row">
        <button class="link-btn" id="btn-share-card">결과 카드 공유</button>
        <button class="link-btn" id="btn-open-delete-play">스테이지 삭제</button>
      </div>
```

- [ ] **Step 2: CSS 규칙 추가**

`index.html:318`(`.eyedropper-badge{...}` 규칙) 바로 다음, `320`의 `/* ---------- 스포이드 확대 루페 ---------- */` 주석 앞에 삽입:

```css

/* ---------- 이어달리기 체인 CTA ---------- */
.relay-hook{ text-align:center; font-size:19px; margin:14px 0 6px; }
.relay-hook b{ color:var(--accent); }
.relay-cta{ font-size:22px; padding:13px 0; animation:relayPulse 2.2s ease-in-out infinite; }
@keyframes relayPulse{
  0%,100%{ box-shadow:var(--shadow); }
  50%{ box-shadow:0 4px 22px rgba(228,91,91,.45); }
}
.panel-links-row{ display:flex; justify-content:center; gap:4px; margin-top:4px; }
.panel-links-row .link-btn{ margin:0; }
```

- [ ] **Step 3: 로컬 서버로 결과 화면 렌더링 확인**

실제 플레이 흐름(사진 업로드→색칠→배치→저장→플레이→클리어)을 거치지 않고, 브라우저 콘솔에서 DOM을 직접 조작해 결과 화면만 노출시켜 스타일을 확인한다.

1. 저장소 루트에서 로컬 서버 실행: `python -m http.server 8765`
2. `mcp__claude-in-chrome__navigate`로 `http://localhost:8765/index.html` 접속
3. `mcp__claude-in-chrome__javascript_tool`(또는 동등 도구)로 다음 스크립트를 실행해 결과 화면을 강제로 표시:

```javascript
document.querySelectorAll('main > section').forEach(s => s.classList.add('hidden'));
document.getElementById('screen-maker').classList.remove('hidden');
document.querySelectorAll('#screen-maker .panel').forEach(p => p.classList.add('hidden'));
document.getElementById('playend-panel').classList.remove('hidden');
document.getElementById('grade-letter').textContent = 'A';
document.getElementById('result-time').textContent = '12.3초';
```

4. `mcp__claude-in-chrome__computer`(action: `screenshot`)로 결과 화면 캡처

Expected: "이번엔 당신 차례예요..." 문구가 표시되고, "나도 숨기기" 버튼이 화면 폭 전체를 채우는 큰 버튼으로 단독 강조되어 있으며, "결과 카드 공유"·"스테이지 삭제"는 하단에 작은 링크 2개로 나란히 표시됨. 레이아웃 깨짐(버튼 겹침, 텍스트 잘림) 없음.

- [ ] **Step 4: 로컬 서버 종료**

Step 3에서 실행한 `python -m http.server 8765` 프로세스를 종료한다 (포트를 점유한 채로 남겨두지 않음).

- [ ] **Step 5: Commit**

```bash
git add index.html
git commit -m "결과 화면 '나도 숨기기' CTA 강화 — 단독 주역 배치, 유도 문구, 펄스 애니메이션 추가"
```

---

## Self-Review 결과
- **스펙 커버리지**: 설계 문서의 4개 변경 사항(시각적 우선순위 재조정, 유도 문구, 레이아웃 순서, 펄스 애니메이션) 모두 Task 1 Step 1~2에서 반영됨.
- **플레이스홀더 스캔**: 없음 — 모든 코드 블록에 실제 값 포함.
- **타입/네이밍 일관성**: 신규 CSS 클래스명(`.relay-hook`, `.relay-cta`, `.panel-links-row`)이 HTML과 CSS 양쪽에서 동일하게 사용됨. 기존 id(`btn-play-make` 등)는 변경하지 않아 JS 핸들러와 계속 일치.
